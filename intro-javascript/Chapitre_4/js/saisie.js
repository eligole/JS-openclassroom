//A

var chiffre = "";
//var chiffre = Number (prompt("entre un nombre entre 50 et 100"));

while ((chiffre <= 50) || (chiffre >= 100)) {
    var chiffre = Number(prompt("entre un nombre entre 50 et 100"));
}

//B

var chiffre = "";
//var chiffre = Number (prompt("entre un nombre entre 50 et 100"));

while ((chiffre >= 50) || (chiffre >= 100)) {
    var chiffre = Number(prompt("entre un nombre entre 50 et 100"));
}
